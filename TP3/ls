# Créer le namespace
kubectl apply -f namespace.yml

# Créer les pods
kubectl apply -f pod-red.yml
kubectl apply -f pod-blue.yml

# Créer le service NodePort
kubectl apply -f service-nodeport-web.yml

# Vérifier que le service a trouvé les deux pods (champ endpoint)
kubectl describe service -n production web-service


